import { HomeView } from '@/features/home/components/home-view';

/**
 * Página inicial do protótipo ObservaTur.
 *
 * Nesta etapa, toda a experiência é orientada por dados mockados
 * para demonstrar arquitetura e UI sem dependências de backend.
 */
export default function HomePage() {
  return (
    <main>
      <HomeView />
    </main>
  );
}
