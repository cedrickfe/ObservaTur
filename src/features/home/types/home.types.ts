export type Highlight = {
  id: string;
  title: string;
  description: string;
};

export type CitySummary = {
  id: string;
  city: string;
  attractions: number;
  events: number;
  satisfaction: string;
};
